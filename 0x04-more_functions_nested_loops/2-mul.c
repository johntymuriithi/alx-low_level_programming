#include <stdio.h>
/**
 * mul -  multiplies two interger values
 * @a: value which is the first operand
 * @b: second operand
 *
 * Return: Always product of a and b
 */
int mul(int a, int b)
{
	return ((a * b));
}
