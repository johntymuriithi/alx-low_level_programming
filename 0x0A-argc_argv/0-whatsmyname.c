#include <stdio.h>
/**
 * main - prints program name folloewd bya new line
 * @argc: number of arguments
 * @argv: arguments passed
 *
 * Return: Always zero
 */
int main(int __attribute__((unused)) argc, char* argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
